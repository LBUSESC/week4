spring:
  application:
    name: student-portal
  datasource:
    url: jdbc:mysql://localhost:3306/student_portal?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080